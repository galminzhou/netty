package io.netty.test.test02;import io.netty.buffer.ByteBuf;import io.netty.buffer.ByteBufOutputStream;import io.netty.buffer.Unpooled;import io.netty.util.CharsetUtil;import io.netty.util.internal.ThreadLocalRandom;public class TestByteBuf {    public static void main(String[] args) {        ByteBuf buf = Unpooled.copiedBuffer("String HelloWorld", CharsetUtil.UTF_8);        System.out.println(buf.readerIndex());        System.out.println(buf.writerIndex());        System.out.println(buf.capacity());        //buf.clear();        System.out.println(buf.capacity());        System.out.println("isReadable:" + buf.isReadable());        ByteBuf sliced = buf.slice();        System.out.println(sliced.toString(CharsetUtil.UTF_8));        buf.setByte(0, (char) 'M');        System.out.println(buf.getByte(0) == sliced.getByte(0));        ByteBuf copy = buf.copy();        System.out.println(copy.toString(CharsetUtil.UTF_8));        buf.setByte(0, 'L');        System.out.println(buf.getByte(0) == copy.getByte(0));    }}